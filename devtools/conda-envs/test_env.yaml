name: autosolvate
channels:
  - openbabel
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
    
    # Openbabel
  - openbabel=2.4.1

    # Molecule visualization
  - nglview

    # Pip-only installs
  - pip:
    - imolecule
  #  - codecov

